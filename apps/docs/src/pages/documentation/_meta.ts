import type {Meta} from 'nextra';

const meta: Meta = {
	context: 'Context',
	routes: 'Routes',
	routers: 'Routers',
	handler: 'Handler',
	streaming: 'Streaming',
	'error-handling': 'Error Handling',
};

export default meta;
